<?php
/**
 * @file
 * Code for the deninet link feature.
 */

include_once 'deninet_link.features.inc';

/**
 * Implements hook_form_FORM_ID_alter().
 */
function deninet_link_form_node_form_alter(&$form, &$form_state, $form_id) {
  if ($form['#node']->type == 'link') {
    // Hide Title field?
  }
}
